/**
 * Button 컴포넌트의 기본 스타일 속성
 */
export enum DefaultStyleProp {
  SIZE,
  LEADING,
  WEIGHT,
  HEIGHT,
  MIN_WIDTH,
  ROUNDED,
  PADDING_X,
  PADDING_Y,
  CUSTOM
}

export type DefaultStylePropType = `${DefaultStyleProp}`
